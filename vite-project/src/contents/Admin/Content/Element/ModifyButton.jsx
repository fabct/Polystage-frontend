const ModifyButton = (props) => {

    return (
            <button
                onClick={props.handleModify}
            >
                Modify
            </button>
    );
}

export default ModifyButton;